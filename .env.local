# Base da API usada pelo frontend (Vite)
# Ajuste para sua URL pública onde as rotas /api estão ativas
VITE_API_BASE_URL=https://bolao-brasileirao-2025.vercel.app

# Base pública do app (usada por webhooks)
PUBLIC_BASE_URL=https://bolao-brasileirao-2025.vercel.app

# Opcional: se você rodar backend local com vercel dev,
# altere para http://localhost:3000
# VITE_API_BASE_URL=http://localhost:3000
# PUBLIC_BASE_URL=http://localhost:3000

# Observação importante:
# As variáveis abaixo são do BACKEND e devem ser configuradas no ambiente do Vercel (Project Settings > Environment Variables):
# TOKEN_ENCRYPTION_KEY=openssl rand -base64 32 (para salvar tokens via OAuth)
# MP_CLIENT_ID=... (Mercado Pago)
# MP_CLIENT_SECRET=... (Mercado Pago)
# MP_OAUTH_REDIRECT_URL=https://bolao-brasileirao-2025.vercel.app/api/oauth/mp/callback
# MP_ACCESS_TOKEN=... (fallback rápido para testes)
# MP_WEBHOOK_SECRET=... (opcional para validar assinatura do webhook)